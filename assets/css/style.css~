/* Google fonts import */
@import url('https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,100;0,300;0,400;0,700;0,900;1,100;1,300;1,400;1,700;1,900&family=Lora&family=Oswald:wght@200;300;400;500;600;700&display=swap');

/* Asterisk wildcard selector to override default styles added by the browser */
* {
    padding: 0;
    margin: 0;
    box-sizing: border-box;
}

/* general styles */
body {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    /* generic project styles */
    color: #ffffff;
    font-family: 'Lato', sans-serif;

}

#container{
	height: 100vh;
	
	overflow: auto;
}

h1, h2, h3 {
    font-family: 'Oswald', sans-serif;
    text-transform: uppercase;
    letter-spacing: 2px;
    color: #ffffff;
}

.hidden-heading{
    display: none;
}

/* header */
header {
    background-color: #000000;
    padding: 0 1rem;
    position: fixed;
    z-index: 99;
    width: 100%;
    box-shadow: 0 5px 10px #3a3a3a;
    display: flex;
    justify-content: space-between;
    align-items: baseline;
}

header a {
    text-decoration: none;
    color: inherit;
}

#menu {
    font-size: 110%;
    letter-spacing: 4px;
    list-style-type: none;
    z-index:1000;
}

#menu>li {
    margin-bottom: 1em;
}

.active {
    border-bottom: 1px solid #ffffff;
}

/*navbar styles with dropdown toggle*/
nav {
    position: absolute;
    background-color: #000000;
    width: 100%;
    left: 0;
    padding: 0 1rem;
    box-shadow: 0 5px 10px #3a3a3a;
    display: none;
    top: 100%;
    z-index: 99;
}

/* nav toggle*/
#nav-toggle:checked~nav {
    display: block;
    
}

#nav-toggle {
    display: none;
}

.nav-toggle-label {
    font-size: 2rem;
    
}

/* main content*/
main {
	margin-top:-125px;
   height: 100%; 
  display: flex;
  flex-direction: column;
}

main > section{
	position: relative;
  	font-size: 1rem;
   width: 100%;
}
/* side listing section */
#side-listing{
	 height: 600px;
	 width:200px;
    position: fixed;
    z-index: 99;
}

#side-listing img{
	display: block;
  margin-left: auto;
  margin-right: auto;
}

#side-listing-content{
    background-color: #000000;
    font-size: 130%;
    position: absolute;
    bottom: 30px;
    width: 100vw;
    padding-top: 20px;
    padding-left: 10px;
}

#side-listing-content > h2, #side-listing-content > h3{
    color: #ffffff;
}

/* main content section*/
#main-content{
	margin-left:10px;
	left:10px;
	top:600px;
	width: 75%;
	height: 100%;
}

#main-content-text h2, #main-content-text h1, #main-content-text h3{
	height: 100%;
	color: #000000;
}
	
#main-content-text{
	color: #000000;
}

#main-content-text ol{
	margin-left: 30px;;
	
}

/* contact form */
#contact{
    padding: 20px 10% 0 10%;
    background-position: 75%;
    flex-grow: 1;
}

#contact-form {
    max-width: 500px;
    padding: 30px;
    color: #ffffff;
    box-shadow: 0 5px 10px #3a3a3a;
    background-color: rgba(60, 60, 60, 0.6);
}

#contact-form h3{
	color: #ffffff;
}

#contact h2 {
    color: #fafafa;
    margin-bottom: 20px;
}

.text-input{
    background: transparent;
    color: #fafafa;
    width: 100%;
    height: 25px;
    margin: 5px 0 20px 0;
    border: 1px solid #fafafa;
    border-radius: 2px;
    padding: 0 5px;
}

.textarea-input{
	 background: transparent;
    color: #fafafa;
    width: 100%;
    height: 250px;
    margin: 5px 0 20px 0;
    border: 1px solid #fafafa;
    border-radius: 2px;
    padding: 0 5px;
}

.contact-button {
    margin-top: 20px;
    border-radius: 2px;
    padding: 15px 32px;
    background-color: #454b1b;
    color: #fafafa;
    font-size: 100%;
}

.text-input:hover{
    border-color: #f16c6b;
}

.contact-buttom:hover{
    background-color: #fafafa;
    color: #454b1b;
}

/* footer */
footer{
	position: fixed;
   left: 0;
   bottom: 0;
   width: 100%;
}

#social-networks {
	color: #ffffff;
	background-color: #000000;
   text-align: center;
   height: 2vh;
   padding: 5px 0;
    
   display: flex;
   justify-content: space-evenly;
   list-style-type: none;
}

#social-networks i {
    font-size: 160%;
    padding: 5%;
    color: #ffffff;
}

/* media query: tablets and larger (768px and up) */
@media screen and (min-width: 768px) {

    /* header */
    nav {
        display: block;
        position: relative;
        box-shadow: none;
        width: fit-content;
        padding-right: 1rem;
    }

    #menu {
        display: flex;
        
    }

    #menu>li {
        padding-left: 1rem;
    }

    .nav-toggle-label {
        display: none;
    }

	#side-listing-content{
    	background-color: #000000;
    	font-size: 130%;
    	position: absolute;
    	bottom: 30px;
    	width: 200px;
    	padding-top: 40px;
    	padding-left: 10px;
	}
	
	#main-content{
		margin-left:10px;
		left:200px;
		top:130px;
		width: 75%;
		height: 100%;
	}
	
	main {
   	margin-top:-20px;
   }
   
   main > section{
   	display: block;
		font-size: 1rem;
  		width: 75%;
	}
    
}

